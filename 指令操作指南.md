# 指令操作指南

## 指令速查表

| 命令 | 说明 |
|------|------|
| `/exit` | 退出 |
| `/clear` | 清空记忆 |
| `/undo [N]` | 回退到第 N 轮（默认回退1轮） |
| `/compress` | 压缩历史（减少 token） |
| `/reload` | 热重载配置和智能体 |
| `/agents` | 查看智能体列表 |
| `/diff` | 查看 AI 的文件改动记录 |
| `/mcp` | 查看已加载的 MCP 工具 |
| `/save [名]` | 保存当前会话 |
| `/load <名>` | 恢复已保存的会话 |
| `/sessions` | 查看所有已保存会话 |
| `/list` | 显示所有指令 |
| `@名称` | 切换智能体 |
| `@名称 消息` | 切换并直接对话 |
| `"""` | 多行输入模式 |
| `Ctrl+Q` | 中断 AI 生成或工具执行 |

## 基本指令

### /exit
退出程序。退出前会自动保存当前会话（autosave）。

### /clear
清空当前对话记忆，重新开始。AI 会忘掉之前的所有对话内容。

### /list
显示所有可用指令列表。

---

## 对话管理

### /undo [N]
回退到第 N 轮对话，撤回第 N+1 轮到当前轮的所有对话记录和文件改动。

- `/undo` — 不带参数，回退 1 轮（撤回最近一轮）
- `/undo 5` — 回退到第 5 轮（撤回第 6 轮到当前的所有改动）

说明：
- 执行前会显示警告和将要恢复的文件列表，需确认后才执行
- 此操作不可撤回
- 编号对应每轮对话前显示的 `[N]`

### /compress
压缩历史记忆，减少 token 占用。当对话轮数较多时，会将早期对话压缩为摘要保留。

- `/compress` — 执行一次压缩

### /save [名称]
保存当前会话（包括对话历史、文件改动记录、轮次信息）。

- `/save` — 使用默认名称 "autosave" 保存
- `/save 项目调试` — 保存为名称 "项目调试"

### /load <名称>
恢复之前保存的会话，加载其对话历史和文件改动记录。

- `/load 项目调试` — 恢复名为 "项目调试" 的会话
- 使用 `/sessions` 查看有哪些可用会话

### /sessions
查看所有已保存的会话列表，显示保存时间、智能体和对话轮数。

---

## 文件改动追踪

### /diff
查看 AI 对文件的改动记录。

- `/diff` — 显示所有改动列表
- `/diff N` — 查看第 N 项的详细 diff
- `/diff N.M` — 折叠项展开后，查看其中第 M 条的详细 diff
- `/diff 路径` — 查看指定文件的改动历史
- `/diff 路径 N` — 查看指定文件第 N 条记录的详细 diff

示例：
```
/diff              → 列出全部改动
/diff 3            → 查看第 3 项详情
/diff 2.1          → 查看第 2 项（折叠组）中第 1 条的 diff
/diff src/main.py  → 查看 main.py 的所有改动
/diff src/main.py 2 → 查看 main.py 第 2 条改动的 diff
```

---

## 智能体相关

### /agents
查看所有已加载的智能体列表。

### /reload
重新加载配置文件和智能体定义，无需重启程序。

### @名称
切换到指定智能体。

- `@coder` — 切换到 coder 智能体
- `@coder 帮我写个排序` — 切换到 coder 并直接发消息

---

## 工具相关

### /mcp
查看已加载的 MCP 工具列表。

---

## 特殊输入

### 多行输入模式
输入 `"""` 开始多行输入，再输入 `"""` 结束：
```
[1] 你: """
  ... (多行模式，输入 """ 结束)
  ... 第一行内容
  ... 第二行内容
  ... """
```

### Ctrl+Q
中断 AI 正在进行的生成或工具执行。
